services:
  scraper:
    build: .
    volumes:
      - ./scraper:/app
    working_dir: /app
    tty: true
    # Uncomment the following line to run the spider
    # command: cd scraper && scrapy crawl example
